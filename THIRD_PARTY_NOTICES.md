# 第三方组件与许可

本项目自身的代码用 **MIT**（见 `LICENSE`）。但**打包出来的 exe 里含别人的东西**，
分发它就要守人家的规矩。下面这些是必须知道的，尤其是 Qt 那条。

| 组件 | 用在哪 | 许可证 | 分发 exe 时的义务 |
|---|---|---|---|
| **Qt / PySide6** | 图形界面 | **LGPLv3** | **见下方，必须处理** |
| lxml | 解析 xml | BSD-3-Clause | 附版权声明与许可文本 |
| PyYAML | 读规则文件 | MIT | 附版权声明与许可文本 |
| PyInstaller | 打包工具（bootloader 会进 exe） | GPL-2.0 **附打包例外** | 例外条款允许自由分发打包结果；项目中说明即可 |
| python-docx | 仅测试用（运行时不依赖） | MIT | 附版权声明 |

## Qt 这条要特别注意（LGPLv3）

你在 GitHub 上发布打包好的 exe，等于**在分发 Qt 的二进制**。LGPLv3 允许这么做，
但要满足几个条件：

1. **附上 LGPLv3 许可全文**。做法：在仓库里放一份
   [`LICENSE-LGPLv3.txt`](https://www.gnu.org/licenses/lgpl-3.0.txt)，
   并在 README 里指向它。
2. **说明用的是哪个 Qt 版本**。本项目打包时用的是 PySide6 6.11.2（对应 Qt 6.11）。
3. **保持动态链接**，让使用者能用自己编译的 Qt 替换它。
   PyInstaller 默认就是把 Qt 以 DLL 形式放在包里（不是静态链接），**这一条自动满足**。
4. **不修改 Qt 本身**。本项目没有改，只是调用。

这几条都做到就合规了。**最容易漏的是第 1 条**——很多人以为"能跑就行"。

如果嫌麻烦，还有一条路：不发布 exe，只发源码，让使用者自己
`pip install -r requirements-gui.txt && python app.py`。那样你就没有分发 Qt 的二进制。

## 数据与版权

- **真实论文样本不入库**：仓库里的代码从不包含任何学生论文的内容。
  回归测试引用的样本文件需要自行准备，文件不在就自动跳过。
- **学校模板不入库**：`rules/` 里的规则是从学校官方模板的批注整理出来的编码结果；
  模板原件（`.docx`）属于学校材料，不随本仓库分发。
- 本工具是**非官方**的个人项目，与中南林业科技大学无关。
